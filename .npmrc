# 設置 npm 日誌級別為 error 以減少警告
loglevel=error

# 使用 npm ci 進行生產構建
engine-strict=true

# 安全的可選依賴處理
include=optional

# 忽略平台檢查錯誤（僅針對可選依賴）
ignore-platform-reqs=false

# 減少構建時的警告輸出
fund=false
audit=false

# 針對 Railway 部署的配置
progress=false 